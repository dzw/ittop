# 2026-09-05

## ittop(Electron 多工作区管理器)新增 .vscode 配置
- 项目: electron-vite 3 + React + electron 43(node-pty)。`npm run dev` = electron-vite dev; dev 端口默认 5173。
- 新建 `.vscode/tasks.json`(dev/build/build:win/start/typecheck/lint/test/test:e2e 八个 npm 任务)与 `.vscode/launch.json`。
- launch 要点: 主进程调试按 electron-vite 官方推荐用 type:node launch 指向 node_modules/.bin/electron-vite.cmd + `--sourcemap`, env 需带 `ELECTRON_RENDERER_URL=http://localhost:5173`(本仓库 src/main/index.ts 依赖该变量加载 dev URL)+ `REMOTE_DEBUGGING_PORT=9222`;渲染进程用 chrome attach 9222; 另含 Debug All compound 与两个 node-terminal 运行项(Run App dev/preview)。
- 已核实 electron-vite v3(node_modules/electron-vite/dist/chunks/lib-*.mjs startElectron)仅通过 env `REMOTE_DEBUGGING_PORT`/`V8_INSPECTOR_PORT`/`V8_INSPECTOR_BRK_PORT` 把调试参数转给 Electron,不再注入 ELECTRON_RENDERER_URL(需自行 env 提供)。
